<!--
  The flag on an Estimated Entry — a meal that was not weighed, so its figures are
  the User's own guess (CONTEXT.md). It rides in a FigureRow's `marker` slot,
  inline after the name, because it qualifies the thing rather than its figures.
-->
<template>
  <UBadge color="warning" variant="subtle" size="xs" class="shrink-0">
    est.
  </UBadge>
</template>
